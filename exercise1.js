function areaTriangle () {
    let base = [4, 10, 18];
    let height = [3, 5, 4];
    console.log((base[0] * height[0]) / 2);
    console.log((base[1] * height[1]) / 2);
    console.log((base[2] * height[2]) / 2);
}

areaTriangle();
